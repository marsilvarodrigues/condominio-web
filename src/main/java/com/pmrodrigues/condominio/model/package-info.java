@FilterDef(
        name = "condominioFilter",
        parameters = @ParamDef(name = "condominioId", type = Long.class)
)
package com.pmrodrigues.condominio.model;

import org.hibernate.annotations.FilterDef;
import org.hibernate.annotations.ParamDef;
